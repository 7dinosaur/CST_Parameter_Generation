# 体积等效截面积计算逻辑

## 1. 概述

本程序 `FABOOM` 用于超声速声爆预测，基于 Whitham F-函数理论。其中**体积等效截面积** $S_V(x)$ 是核心中间变量，表示飞行器几何外形在来流马赫锥平面上的等效截面积沿机身轴线 $x$ 的分布。总等效截面积为：

$$S(x) = S_V(x) + S_L(x)$$

其中 $S_L(x)$ 为升致等效截面积（由气动求解器 A502 提供）。

---

## 2. 主程序调用流程

```text
main FABOOM
  │
  ├─ call A502in / A502in_2     → 生成 A502 气动输入文件
  ├─ 执行 A502.exe              → 运行面元法气动求解
  ├─ call A502out1 / A502out2   → 后处理气动结果，提取升致面积 S_L(x)
  ├─ call slice                 → 计算体积等效截面积 S_V(x)
  ├─ call area_distribution1/2  → 合成总等效截面积 S(x)=S_V(x)+S_L(x)
  ├─ call diferrential          → 计算 S(x) 的二阶导数
  └─ call F_function            → 计算 Whitham F-函数
```

---

## 3. 体积等效截面积计算 —— `slice` 子程序

**位置**: `FABOOM.f90` 第 2106–2363 行

**输入**:
- `tempdata/geometry.dat` — 多区结构化表面网格
- `indata/FABoom.in` — 来流参数（马赫数 Ma、迎角 AoA、参考长度 L 等）

**输出**:
- `slice/Volume area.dat` — $(x, S_V(x))$ 数据点
- `slice/rotate.dat` — 旋转后的几何

### 3.1 参数读取与初始化

| 参数 | 符号 | 来源 |
|------|------|------|
| 马赫数 | $M_\infty$ | `FABoom.in` |
| 迎角 | $\alpha$ (AoA) | `FABoom.in` |
| 机身长度 | $L$ | `FABoom.in` |
| 最大半宽 | $A_H$ | `FABoom.in` |
| 参考面积 | $S$ | `FABoom.in` |
| 参考高度（线下距离） | $H = -30$ | 硬编码 |
| 切片数量 | $N_{slice} = 150$ | 硬编码 |

### 3.2 坐标旋转（迎角变换）

对原始几何坐标 $(x,y,z)$ 绕 $y$ 轴旋转迎角 $\alpha$：

$$\begin{bmatrix} x_t \\ y_t \\ z_t \end{bmatrix} = \begin{bmatrix} \cos\alpha & 0 & \sin\alpha \\ 0 & 1 & 0 \\ -\sin\alpha & 0 & \cos\alpha \end{bmatrix} \begin{bmatrix} x \\ y \\ z \end{bmatrix}$$

### 3.3 马赫锥平面投影

将旋转后的坐标沿马赫锥向下投影至参考高度 $H$：

$$x_{ty}(i,j) = x_t(i,j) + \left(z_t(i,j) - H\right) \cdot \sqrt{M_\infty^2 - 1}$$

其中 $\beta = \sqrt{M_\infty^2 - 1}$ 为 Prandtl-Glauert 因子，$H=-30$ 为参考线（飞机下方 30 单位长度处）。

此投影的含义是：从马赫线与纵轴交点处观察，飞行器表面同一点到观察者处的相位位置由 $x_{ty}$ 决定。

### 3.4 切片位置定义

沿投影后的 $x_{ty}$ 轴等间距布置 150 个切片面：

$$x_{slice}(i) = -0.15L + \frac{L + 0.5L + \Delta x}{N_{slice} - 1} \cdot (i-1) - H \cdot \sqrt{M_\infty^2 - 1}$$

其中 $\Delta x = A_H \cdot \sqrt{M_\infty^2 - 1}$。

### 3.5 求交 —— 切片面与网格的交点

对每个网格区 `k`，对每个切片 `n`，在网格单元的四类边上查找与切片平面的交点：

#### (a) 沿 $i$ 方向（展向）边（第 j 列内部）
条件：$x_{ty}(i,j)$ 与 $x_{ty}(i,j+1)$ 跨越 $x_{slice}(n)$。

线性插值求交点：

$$\lambda = \frac{x_{slice}(n) - x_{ty}(i,j)}{x_{ty}(i,j+1) - x_{ty}(i,j)}$$

$$\begin{aligned} x_{cross} &= x_t(i,j) + \lambda \cdot (x_t(i,j+1) - x_t(i,j)) \\ y_{cross} &= y_t(i,j) + \lambda \cdot (y_t(i,j+1) - y_t(i,j)) \\ z_{cross} &= z_t(i,j) + \lambda \cdot (z_t(i,j+1) - z_t(i,j)) \end{aligned}$$

#### (b) 前缘边（第 1 列，$i \to i+1$）
条件：$x_{ty}(i,1)$ 与 $x_{ty}(i+1,1)$ 跨越 $x_{slice}(n)$。

#### (c) 后缘边（第 $N_{column}$ 列，$i \to i+1$）
条件：$x_{ty}(i,N_{col})$ 与 $x_{ty}(i+1,N_{col})$ 跨越 $x_{slice}(n)$。

#### (d) 端点恰好等于切片位置
$x_{ty}(i,N_{col}) = x_{slice}(n)$ 时直接记录。

### 3.6 截面积计算

对每个切片，将找到的 $N_{id}$ 个交点按其空间位置（隐式按 $y,z$ 顺序排列），用梯形公式计算封闭曲线围成的面积：

$$S_{slice}(n) = \frac{1}{2}\sum_{i=1}^{N_{id}-1} \left(y_{cross}(i) + y_{cross}(i+1)\right) \cdot \left(z_{cross}(i) - z_{cross}(i+1)\right)$$

这是多边形面积公式（对 $z$ 轴的积分形式）。

### 3.7 后处理

1. **还原 $x$ 坐标**：$x_{slice}(n) = x_{slice}(n) + H \cdot \beta$（去掉投影偏移）
2. **对称加倍**：$S_{slice}(n) = 2 \cdot S_{slice}(n)$（利用左右对称性，原计算仅为半模面积）
3. **裁剪首尾零值**：找到第一个非零和最后一个非零的切片索引 $X_{start}$、$X_{end}$
4. **平移 $x$ 坐标**：使 $x(X_{start}) = 0$
5. **输出**：写入 `slice/Volume area.dat`，格式为：
   ```
   有效点数 N
   x(1)  S_V(1)
   x(2)  S_V(2)
   ...
   ```

---

## 4. 总等效截面积合成

### 4.1 `area_distribution1`（仅体积贡献，KG=1 模式）

**位置**: `FABOOM.f90` 第 2469–2559 行

直接读取 `slice/Volume area.dat` 并原样写出，$S(x) = S_V(x)$。

输出文件：
- `area/Area_due_to_Volume.dat`
- `area/Total_equivalent_area.dat`
- `sonicboom/sonicboom area.dat`

### 4.2 `area_distribution2`（体积+升力贡献，KG=2 模式）

**位置**: `FABOOM.f90` 第 2380–2465 行

1. 读取 `slice/Volume area.dat` → $(x_v, S_V)$
2. 读取 `A502/Lift area.dat` → $(x_L, S_L)$
3. 将两者插值到统一的 200 个等间距 $x$ 坐标上
4. 计算总等效截面积：

$$S(i) = S_V(i) + S_L(i)$$

5. 输出到：
   - `area/Area_due_to_Volume.dat` — 仅体积分量
   - `area/Area_due_to_Lift.dat` — 仅升力分量
   - `area/Total_equivalent_area.dat` — 总等效截面积
   - `sonicboom/sonicboom area.dat` — 用于声爆计算的面积分布

---

## 5. 升致等效截面积 —— `A502out1` 子程序

**位置**: `FABOOM.f90` 第 1090–1588 行

从 A502 面元法求解器输出中提取截面压力分布，积分得到展向升力分布：

$$\Delta C_p(i) = \int_{x_{lower}}^{x_{upper}} C_p(x) \, dx$$

展向升力：

$$F_z(j) = \frac{1}{2} \rho_\infty (M_\infty c_\infty)^2 \cdot \frac{\Delta C_p(j) + \Delta C_p(j+1)}{2} \cdot (y_{j+1} - y_j)$$

升致等效截面积 $S_L(x)$ 基于 Whitham 理论由升力分布沿展向积分得到，输出到 `A502/Lift area.dat`。

---

## 6. 后续处理

### 6.1 二阶导数 —— `diferrential`

**位置**: `FABOOM.f90` 第 2570–2653 行

对 $S(x)$ 求二阶中心差分：

$$S''(x_i) = \frac{S(x_{i+1}) + S(x_{i-1}) - 2S(x_i)}{(x_{i+1} - x_i)^2}$$

### 6.2 F-函数 —— `F_function`

**位置**: `FABOOM.f90` 第 2662–2820 行

基于 Whitham 理论计算 F-函数：

$$F(y) = \frac{1}{2\pi} \int_0^y \frac{S''(x)}{\sqrt{y - x}} \, dx$$

离散形式（梯形积分）：

$$F_j = F_{j-1} + \frac{1}{2}(x_i - x_{i-1}) \left( \frac{S''(x_i)}{\sqrt{y_j - x_i}} + \frac{S''(x_{i-1})}{\sqrt{y_j - x_{i-1}}} \right)$$

---

## 7. 数学总结

```
几何网格 (x,y,z)
    │
    ▼ 绕y轴旋转迎角α
旋转坐标 (x_t, y_t, z_t)
    │
    ▼ 马赫锥投影: x_ty = x_t + (z_t - H)·√(M²-1)
投影坐标
    │
    ▼ 150个切片面求交 → 线性插值
交点集 {x_cross, y_cross, z_cross}_n
    │
    ▼ 多边形面积公式(梯形法)
每个切片的半模面积 s_slice(n)
    │
    ▼ ×2 (对称) → 裁剪首尾 → 输出
S_V(x) 体积等效截面积分布
    │
    ▼ + S_L(x) 升致等效截面积
S(x) = S_V(x) + S_L(x) 总等效截面积分布
    │
    ▼ 二阶中心差分
S″(x)
    │
    ▼ Whitham积分
F(y)  → 远场声爆信号
```

---

## 8. 关键参数一览

| 参数 | 值 | 位置 | 说明 |
|------|-----|------|------|
| $H$ | -30 | `slice` L2110, `A502out1` L1101 | 马赫锥投影参考高度 |
| $N_{slice}$ | 150 | `slice` L2145 | 切片数量 |
| $\beta = \sqrt{M^2-1}$ | 由Ma决定 | 多处 | Prandtl-Glauert因子 |
| $cha$ | 200 | `area_distribution2` L2408 | 统一插值点数 |
| $\gamma$ | 1.4 | 多处 | 比热比 |
